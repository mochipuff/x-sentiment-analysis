# OAuth 2.0 — app-only (use for searching public tweets)
X_BEARER_TOKEN=
X_CLIENT_ID=
X_CLIENT_SECRET=
X_REFRESH_TOKEN=

# OAuth 1.0a — user context (needed for posting tweets, reading DMs, etc.)
X_CONSUMER_KEY=
X_CONSUMER_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
